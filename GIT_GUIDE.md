# 🚀 Git 推送指南

## 📋 当前状态

✅ Git 仓库已初始化  
✅ 远程仓库已添加：https://github.com/voloayudan-cpu/claude.git  
✅ 所有文件已提交  
⏳ 等待推送到GitHub

## 🔧 推送到 GitHub

### 方法一：使用自动化脚本（推荐）

1. **双击运行** `git-push.bat`
2. **输入提交信息**（或直接回车使用默认信息）
3. **等待推送完成**

### 方法二：手动命令

在项目目录中打开终端，运行以下命令：

```bash
# 1. 添加所有更改
git add .

# 2. 提交更改
git commit -m "您的提交信息"

# 3. 推送到GitHub
git push origin main
```

### 方法三：使用 Git Bash

1. 在项目文件夹中右键
2. 选择"Git Bash Here"
3. 运行上述命令

## 📝 提交信息建议

使用有意义的提交信息，例如：

```
feat: 添加新功能
fix: 修复bug
docs: 更新文档
style: 代码格式调整
refactor: 代码重构
test: 添加测试
chore: 构建/工具链更新
```

## 🔄 后续更新流程

每次更新项目后，只需：

1. **运行自动化脚本**：双击 `git-push.bat`
2. **输入提交信息**：描述本次更改
3. **完成**：代码自动推送到GitHub

## 🐛 常见问题

### Q: 推送失败，提示"Connection was reset"
A: 网络连接问题，请：
1. 检查网络连接
2. 尝试使用VPN
3. 稍后重试

### Q: 提示"Authentication failed"
A: 需要配置GitHub认证：
1. 访问 https://github.com/settings/tokens
2. 生成新的Personal Access Token
3. 使用Token代替密码

### Q: 提示"Updates were rejected"
A: 远程仓库有新的提交，需要先拉取：
```bash
git pull origin main --rebase
git push origin main
```

### Q: 如何查看远程仓库状态？
A: 运行以下命令：
```bash
git status
git remote -v
```

## 📊 查看提交历史

```bash
# 查看提交历史
git log --oneline

# 查看详细提交历史
git log

# 查看图形化提交历史
git log --graph --oneline --all
```

## 🔙 撤销操作

### 撤销最后一次提交（保留更改）
```bash
git reset --soft HEAD~1
```

### 撤销最后一次提交（删除更改）
```bash
git reset --hard HEAD~1
```

### 撤销已推送的提交
```bash
git revert HEAD
git push origin main
```

## 📱 GitHub Desktop

如果您更喜欢图形界面：

1. 下载并安装 [GitHub Desktop](https://desktop.github.com/)
2. 克隆仓库：https://github.com/voloayudan-cpu/claude.git
3. 在GitHub Desktop中管理提交和推送

## 🌐 在线查看

推送成功后，访问以下地址查看您的代码：
https://github.com/voloayudan-cpu/claude.git

## 📚 更多资源

- [Git 官方文档](https://git-scm.com/doc)
- [GitHub 官方文档](https://docs.github.com/)
- [Git 教程](https://www.atlassian.com/git/tutorials)

---

**提示**：建议定期推送代码，避免丢失更改！